diff --git a/CMakeLists.txt b/CMakeLists.txt
index fab604ab..ee4ee42d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -28,6 +28,8 @@ option(BUILD_QT5OPENGL "Build with Qt5 OpenGL module" ON)
 option(BUILD_QML "Build QML interfaces" ON)
 option(BUILD_Vulkan "Build Vulkan Renderer" ON)
 
+include(FeatureSummary)
+
 list(APPEND CMAKE_FIND_ROOT_PATH ${QTDIR})
 
 find_package(Qt5Core REQUIRED)
@@ -76,7 +78,7 @@ if(NOT CMAKE_INSTALL_PREFIX_INITIALIZED_TO_DEFAULT)
   set(QTAV_INSTALL_HEADERS ${CMAKE_INSTALL_PREFIX}/include)
   set(QTAV_INSTALL_LIBS ${CMAKE_INSTALL_PREFIX}/lib)
   set(QTAV_INSTALL_BINS ${CMAKE_INSTALL_PREFIX}/bin)
-  set(QTAV_INSTALL_QML ${QTAV_INSTALL_LIBS}/qml)
+  set(QTAV_INSTALL_QML ${QT_INSTALL_QML})
 else()
   set(CMAKE_INSTALL_PREFIX ${QT_INSTALL_PREFIX} CACHE PATH "default install path" FORCE)
   set(QTAV_INSTALL_HEADERS ${QT_INSTALL_HEADERS})
@@ -135,4 +137,4 @@ if(BUILD_EXAMPLES OR BUILD_PLAYERS)
   add_subdirectory(examples)
 endif()
 
-
+feature_summary(WHAT ALL INCLUDE_QUIET_PACKAGES FATAL_ON_MISSING_REQUIRED_PACKAGES)
diff --git a/examples/CMakeLists.txt b/examples/CMakeLists.txt
index 2f7ad26a..de3d3da4 100644
--- a/examples/CMakeLists.txt
+++ b/examples/CMakeLists.txt
@@ -19,7 +19,7 @@ set(COMMON_HEADERS
 if(NOT IOS)
   list(APPEND COMMON_SRC common/ScreenSaver.cpp)
   if(APPLE)
-    list(APPEND COMMON_LIBS "-framework CoreServices")
+    list(APPEND COMMON_LIBS "-framework CoreServices -framework IOKit")
   endif()
 endif()
 
diff --git a/examples/player/MainWindow.cpp b/examples/player/MainWindow.cpp
index 3b7d031a..2fad87b2 100644
--- a/examples/player/MainWindow.cpp
+++ b/examples/player/MainWindow.cpp
@@ -115,11 +115,13 @@ MainWindow::MainWindow(QWidget *parent) :
   , m_shader(NULL)
   , m_glsl(NULL)
 {
-    #if defined(Q_OS_MACX) && QT_VERSION >= QT_VERSION_CHECK(5, 0, 0)
-        QApplication::setStyle(QStyleFactory::create("Fusion"));
-    #endif
+//     #if defined(Q_OS_MACX) && QT_VERSION >= QT_VERSION_CHECK(5, 0, 0)
+//         QApplication::setStyle(QStyleFactory::create("Fusion"));
+//     #endif
 
+#ifndef Q_OS_MACOS
     setWindowIcon(QIcon(QString::fromLatin1(":/QtAV.svg")));
+#endif
     mpOSD = new OSDFilter(this);
     mpSubtitle = new SubtitleFilter(this);
     mpChannelAction = 0;
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 7781fd63..a9c7234b 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -438,16 +438,16 @@ if(HAVE_OPENSL)
   list(APPEND EXTRA_DEFS -DQTAV_HAVE_OPENSL=1)
   list(APPEND EXTRA_LIBS OpenSLES)
 endif()
-check_library_exists(va vaInitialize "" HAVE_VAAPI)
-if(HAVE_VAAPI)
-  list(APPEND SOURCES
-    vaapi/vaapi_helper.cpp
-    vaapi/SurfaceInteropVAAPI.cpp
-    codec/video/VideoDecoderVAAPI.cpp
-  )
-  list(APPEND EXTRA_DEFS -DQTAV_HAVE_VAAPI=1)
-  list(APPEND EXTRA_LIBS va X11)
-endif()
+# check_library_exists(va vaInitialize "" HAVE_VAAPI)
+# if(HAVE_VAAPI)
+#   list(APPEND SOURCES
+#     vaapi/vaapi_helper.cpp
+#     vaapi/SurfaceInteropVAAPI.cpp
+#     codec/video/VideoDecoderVAAPI.cpp
+#   )
+#   list(APPEND EXTRA_DEFS -DQTAV_HAVE_VAAPI=1)
+#   list(APPEND EXTRA_LIBS va X11)
+# endif()
 
 if(NOT APPLE)
   list(APPEND EXTRA_DEFS -DQTAV_HAVE_CUDA=1)
@@ -513,7 +513,7 @@ endforeach()
 
 set_target_properties(${MODULE} PROPERTIES
   MACOSX_RPATH ON
-  FRAMEWORK ON
+#   FRAMEWORK ON
     #  MACOSX_FRAMEWORK_INFO_PLIST qtav-Info.plist
     #PUBLIC_HEADER ${SDK_HEADERS}
     #PRIVATE_HEADER ${SDK_PRIVATE_HEADERS}
diff --git a/widgets/CMakeLists.txt b/widgets/CMakeLists.txt
index 7e380c85..f81ec177 100644
--- a/widgets/CMakeLists.txt
+++ b/widgets/CMakeLists.txt
@@ -1,7 +1,6 @@
 set(MODULE QtAVWidgets)
 set(EXTRA_DEFS -DBUILD_QTAVWIDGETS_LIB)
 list(APPEND EXTRA_DEFS -DQTAV_HAVE_GL=1)
-list(APPEND EXTRA_DEFS -DQTAV_HAVE_VULKAN=1)
 
 set(HAVE_QT5OPENGL 0)
 if(BUILD_QT5OPENGL)
@@ -16,6 +15,7 @@ if(BUILD_Vulkan)
   find_package(Vulkan)
   if(Vulkan_FOUND)
     set(HAVE_Vulkan 1)
+    list(APPEND EXTRA_DEFS -DQTAV_HAVE_VULKAN=1)
   endif()
 endif()
 
@@ -28,7 +28,6 @@ set(SDK_HEADERS
   QtAVWidgets/version.h
   QtAVWidgets/VideoPreviewWidget.h
   QtAVWidgets/WidgetRenderer.h
-  QtAVWidgets/VulkanRenderer.h
 )
 file(GLOB SDK_PRIVATE_HEADERS QtAVWidgets/*.h)
 
@@ -38,7 +37,6 @@ set(SOURCES
   OpenGLWidgetRenderer.cpp
   VideoPreviewWidget.cpp
   WidgetRenderer.cpp
-  VulkanRenderer.cpp
 )
 
 if(HAVE_QT5OPENGL)
@@ -79,7 +77,7 @@ if(HAVE_VULKAN)
 endif()
 set_target_properties(${MODULE} PROPERTIES
   MACOSX_RPATH ON
-  FRAMEWORK ON
+#   FRAMEWORK ON
   VERSION ${PROJECT_VERSION}
   SOVERSION ${SO_VERSION}
   OUTPUT_NAME ${MODULE}
