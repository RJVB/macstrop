diff --git a/src/modules/OpenMPT/OpenMPTVisPatterns.cpp b/src/modules/OpenMPT/OpenMPTVisPatterns.cpp
index d1fdff5e..93a32caf 100644
--- a/src/modules/OpenMPT/OpenMPTVisPatterns.cpp
+++ b/src/modules/OpenMPT/OpenMPTVisPatterns.cpp
@@ -34,7 +34,11 @@ static int computeMaxCellWidth(openmpt::module *module, int pattern, const QFont
     for (int ch = 0; ch < numChannels; ++ch)
     {
         const std::string text = module->format_pattern_row_channel(pattern, 0, ch);
+#if QT_VERSION >= QT_VERSION_CHECK(5, 11, 0)
         const int w = fm.horizontalAdvance(QString::fromStdString(text)) + fm.horizontalAdvance("X");
+#else
+        const int w = fm.width(QString::fromStdString(text)) + fm.width("X");
+#endif
         if (w > maxW)
             maxW = w;
     }
@@ -80,7 +84,11 @@ OpenMPTVisPatterns::OpenMPTVisPatterns(openmpt::module *module, int channels, in
     m_rowHeight = fm.height() + 2;
     m_height = m_rows * m_rowHeight;
     m_maxCellWidth = maxCellWidth;
+#if QT_VERSION >= QT_VERSION_CHECK(5, 11, 0)
     m_charWidth = fm.horizontalAdvance("X");
+#else
+    m_charWidth = fm.width("X");
+#endif
 
     for (int i = 33; i <= 126; ++i)
     {
diff --git a/src/modules/OpenMPT/OpenMPTVisSamples.cpp b/src/modules/OpenMPT/OpenMPTVisSamples.cpp
index 0f84632f..3b9d0b95 100644
--- a/src/modules/OpenMPT/OpenMPTVisSamples.cpp
+++ b/src/modules/OpenMPT/OpenMPTVisSamples.cpp
@@ -43,7 +43,11 @@ static QFont computeFitFont(int nameWidth, int &outCharWidth, int &outAscent)
         fm = QFontMetrics(font);
     }
 
+#if QT_VERSION >= QT_VERSION_CHECK(5, 11, 0)
     outCharWidth = fm.horizontalAdvance(QStringLiteral("X"));
+#else
+    outCharWidth = fm.width(QStringLiteral("X"));
+#endif
     outAscent = fm.ascent();
     return font;
 }
diff --git a/src/qmplay2/YouTubeDL.cpp b/src/qmplay2/YouTubeDL.cpp
index e0a5481e..0c21f275 100644
--- a/src/qmplay2/YouTubeDL.cpp
+++ b/src/qmplay2/YouTubeDL.cpp
@@ -217,6 +217,54 @@ void YouTubeDL::addr(const QString &url, const QString &param, QString *streamUr
     }
 }
 
+#if (QT_VERSION < QT_VERSION_CHECK(5, 15, 0))
+// Splits the string \a command into a list of tokens, and returns the list.
+//
+// Tokens with spaces can be surrounded by double quotes; three
+// consecutive double quotes represent the quote character itself.
+//
+// Copied from Qt 5.15.2
+static QStringList splitCommand(QString &command)
+{
+    QStringList args;
+    QString tmp;
+    int quoteCount = 0;
+    bool inQuote = false;
+
+    // handle quoting. tokens can be surrounded by double quotes
+    // "hello world". three consecutive double quotes represent
+    // the quote character itself.
+    for (int i = 0; i < command.size(); ++i) {
+        if (command.at(i) == QLatin1Char('"')) {
+            ++quoteCount;
+            if (quoteCount == 3) {
+                // third consecutive quote
+                quoteCount = 0;
+                tmp += command.at(i);
+            }
+            continue;
+        }
+        if (quoteCount) {
+            if (quoteCount == 1)
+                inQuote = !inQuote;
+            quoteCount = 0;
+        }
+        if (!inQuote && command.at(i).isSpace()) {
+            if (!tmp.isEmpty()) {
+                args += tmp;
+                tmp.clear();
+            }
+        } else {
+            tmp += command.at(i);
+        }
+    }
+    if (!tmp.isEmpty())
+        args += tmp;
+
+    return args;
+}
+#endif
+
 QStringList YouTubeDL::exec(const QString &url, const QStringList &args, QString *silentErr, bool rawOutput)
 {
     if (!prepare())
@@ -234,7 +282,11 @@ QStringList YouTubeDL::exec(const QString &url, const QStringList &args, QString
     {
         if (auto ap = QMPSettings.getString("YtDl/AdditionalParams").simplified(); !ap.isEmpty())
         {
+#if QT_VERSION >= QT_VERSION_CHECK(5, 15, 0)
             processArgs += QProcess::splitCommand(ap);
+#else
+            processArgs += splitCommand(ap);
+#endif
         }
     }
 
