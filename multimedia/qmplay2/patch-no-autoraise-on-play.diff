commit 619eedfce45dff2d18e331b4332ea31d9f6cc70b
Author: bertin <macportsuser@macports.org>
Date:   Wed Jul 1 13:23:09 2026 +0200

    /opt/local/site-ports/multimedia/qmplay2/files/patch-no-autoraise-on-play.diff

diff --git a/src/gui/MainWidget.cpp b/src/gui/MainWidget.cpp
index bead0070..803e513e 100644
--- a/src/gui/MainWidget.cpp
+++ b/src/gui/MainWidget.cpp
@@ -711,8 +711,10 @@ void MainWidget::videoStarted(bool noVideo)
     if (autoRestoreMainWindowOnVideo || (noVideo && autoOpenVideoWindow))
     {
         if (!videoDock->isVisible())
+        {
             videoDock->show();
-        videoDock->raise();
+            videoDock->raise();
+        }
     }
     if (autoRestoreMainWindowOnVideo)
     {
@@ -720,11 +722,11 @@ void MainWidget::videoStarted(bool noVideo)
         {
             toggleVisibility();
         }
-        else
-        {
-            activateWindow();
-            raise();
-        }
+//         else
+//         {
+//             activateWindow();
+//             raise();
+//         }
     }
     m_restoreWindowOnVideo = false;
 }
