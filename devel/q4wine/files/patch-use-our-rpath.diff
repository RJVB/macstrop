--- src/orig.CMakeLists.txt	2017-07-28 22:34:46.816536833 +0200
+++ src/CMakeLists.txt	2017-07-28 22:36:09.584056159 +0200
@@ -189,14 +189,15 @@
 # which point to directories outside the build tree to the install RPATH
 #SET(CMAKE_INSTALL_RPATH_USE_LINK_PATH TRUE)
 
-IF( NOT ${CMAKE_SYSTEM_NAME} MATCHES Darwin )
-    IF( NOT DEFINED SKIP_RPATH )
-        SET(CMAKE_INSTALL_RPATH "${LIBS_ENTRY_PATH}")
-    ELSE( NOT DEFINED SKIP_RPATH )
-        MESSAGE(STATUS "NOTE: Skip for RPATH build!")
-        SET(CMAKE_SKIP_BUILD_RPATH  TRUE)
-    ENDIF( NOT DEFINED SKIP_RPATH )
-ENDIF( NOT ${CMAKE_SYSTEM_NAME} MATCHES Darwin )
+#IF( NOT ${CMAKE_SYSTEM_NAME} MATCHES Darwin )
+#    IF( NOT DEFINED SKIP_RPATH )
+#        SET(CMAKE_INSTALL_RPATH "${LIBS_ENTRY_PATH}")
+#    ELSE( NOT DEFINED SKIP_RPATH )
+#        MESSAGE(STATUS "NOTE: Skip for RPATH build!")
+#        SET(CMAKE_SKIP_BUILD_RPATH  TRUE)
+#    ENDIF( NOT DEFINED SKIP_RPATH )
+#ENDIF( NOT ${CMAKE_SYSTEM_NAME} MATCHES Darwin )
+message(WARNING "Using predefined CMAKE_INSTALL_RPATH=${CMAKE_INSTALL_RPATH}")
 
 IF( NOT LINGUAS )
 	SET ( LINGUAS af cs de en es fa he it ja pl pt_BR ru uk zh_TW zh_CN )
