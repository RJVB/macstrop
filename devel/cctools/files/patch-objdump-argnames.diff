diff --git otool/orig.main.c otool/main.c
index 54572db..822c29f 100644
--- otool/orig.main.c
+++ otool/main.c
@@ -822,59 +822,59 @@ enum bool version)
 
 	reset_execute_list();
 	add_execute_list(objdump);
-	add_execute_list("-macho");
+	add_execute_list("--macho");
 
 	if(fflag)
-	    add_execute_list("-universal-headers");
+	    add_execute_list("--universal-headers");
 	if(aflag){
-	    add_execute_list("-archive-headers");
+	    add_execute_list("--archive-headers");
 	    if(Vflag)
-		add_execute_list("-archive-member-offsets");
+		add_execute_list("--archive-member-offsets");
 	}
 	if(hflag && !lflag)
-	    add_execute_list("-private-header");
+	    add_execute_list("--private-header");
 	if(lflag)
-	    add_execute_list("-private-headers");
+	    add_execute_list("--private-headers");
 	if(xflag){
 	    if(vflag)
-		add_execute_list("-disassemble-all");
+		add_execute_list("--disassemble-all");
 	    else{
-		add_execute_list("-section");
+		add_execute_list("--section");
 		add_execute_list(",__text");
 	    }
 	}
 	else if(tflag){
 	    if(vflag)
-		add_execute_list("-disassemble");
+		add_execute_list("--disassemble");
 	    else{
-		add_execute_list("-section");
+		add_execute_list("--section");
 		add_execute_list("__TEXT,__text");
 	    }
 	}
 	if(tflag || xflag || segname != NULL){
-	    add_execute_list("-full-leading-addr");
-	    add_execute_list("-print-imm-hex");
+	    add_execute_list("--full-leading-addr");
+	    add_execute_list("--print-imm-hex");
 	}
 	if(pflag != NULL){
-	    add_execute_list("-dis-symname");
+	    add_execute_list("--dis-symname");
 	    add_execute_list(pflag);
 	}
 	if(*mcpu != '\0')
-	    add_execute_list(makestr("-mcpu=", mcpu, NULL));
+	    add_execute_list(makestr("--mcpu=", mcpu, NULL));
 	if(Iflag)
-	    add_execute_list("-indirect-symbols");
+	    add_execute_list("--indirect-symbols");
 	if(Gflag)
-	    add_execute_list("-data-in-code");
+	    add_execute_list("--data-in-code");
 	if(Cflag)
-	    add_execute_list("-link-opt-hints");
+	    add_execute_list("--link-opt-hints");
 	if(Pflag)
-	    add_execute_list("-info-plist");
+	    add_execute_list("--info-plist");
 	if(segname != NULL && sectname != NULL){
-	    add_execute_list("-section");
+	    add_execute_list("--section");
 	    add_execute_list(makestr(segname, ",", sectname, NULL));
 	}
 	if(dflag){
-	    add_execute_list("-section");
+	    add_execute_list("--section");
 	    add_execute_list("__DATA,__data");
 	}
 	if(rflag)
@@ -882,33 +882,33 @@ enum bool version)
 	if(Sflag)
 	    error("for -S functionality, use llvm-nm with -print-armap");
 	if(version)
-	    add_execute_list("-version");
+	    add_execute_list("--version");
 	if(print_bind_info)
-	    add_execute_list("-bind");
+	    add_execute_list("--bind");
 	if(Lflag)
-	    add_execute_list("-dylibs-used");
+	    add_execute_list("--dylibs-used");
 	if(Dflag)
-	    add_execute_list("-dylib-id");
+	    add_execute_list("--dylib-id");
 	if(oflag)
-	    add_execute_list("-objc-meta-data");
+	    add_execute_list("--objc-meta-data");
 
 	if(!vflag)
-	    add_execute_list("-non-verbose");
+	    add_execute_list("--non-verbose");
 	if(!Vflag && (tflag || xflag || segname != NULL))
-	    add_execute_list("-no-symbolic-operands");
+	    add_execute_list("--no-symbolic-operands");
 	if((!jflag && (tflag || xflag || segname != NULL)) || no_show_raw_insn)
-	    add_execute_list("-no-show-raw-insn");
+	    add_execute_list("--no-show-raw-insn");
 	if(Xflag){
-	    add_execute_list("-no-leading-addr");
-	    add_execute_list("-no-leading-headers");
+	    add_execute_list("--no-leading-addr");
+	    add_execute_list("--no-leading-headers");
 	}
 
 	if(all_archs){
-	    add_execute_list("-arch");
+	    add_execute_list("--arch");
 	    add_execute_list("all");
 	}
 	for(i = 0; i < narch_flags; i++){
-	    add_execute_list("-arch");
+	    add_execute_list("--arch");
 	    add_execute_list(arch_flags[i].name);
 	}
 
diff --git man/orig.llvm-otool.1 man/llvm-otool.1
index 9885e8e..c984f3d 100644
--- man/orig.llvm-otool.1
+++ man/llvm-otool.1
@@ -46,7 +46,7 @@ This always implies the
 option, as the
 .IR objdump (1)
 option
-.B \-private-headers
+.B \-\-private-headers
 prints both the mach header and load commands.
 .TP
 .B \-S
@@ -100,7 +100,7 @@ option to print the same information.
 To use these options with
 .IR objdump (1)
 the
-.B \-macho
+.B \-\-macho
 option must be used.
 The default for
 .IR objdump (1)
@@ -114,14 +114,14 @@ option is used.
 .B \-a
 The
 .IR objdump (1)
-.B \-archive-headers
+.B \-\-archive-headers
 will print the archive header, if the file is an archive.  To get the offsets
 to the archive headers that would be printed with the
 .IR otool (1)
 .B \-aV
 options, the
 .IR objdump (1)
-.B \-archive-member-offsets
+.B \-\-archive-member-offsets
 can be added.
 .TP
 .B \-S
@@ -138,19 +138,19 @@ option.
 The
 .IR objdump (1)
 option to display the universal headers is
-.BR \-universal-headers .
+.BR \-\-universal-headers .
 .TP
 .B \-h
 The
 .IR objdump (1)
 option to display the Mach header is
-.BR \-private-header .
+.BR \-\-private-header .
 .TP
 .B \-l
 The
 .IR objdump (1)
 option to display the load commands is
-.B \-private-headers
+.B \-\-private-headers
 which also always displays the Mach header.
 .TP
 .B \-L
@@ -159,13 +159,13 @@ The
 option to display the names and version numbers of the shared libraries that
 the object file uses, as well as the shared library ID if the file is a shared
 library is
-.BR \-dylibs-used .
+.BR \-\-dylibs-used .
 .TP
 .B \-D
 The
 .IR objdump (1)
 option to display just the install name of a shared library is
-.BR \-dylib-id .
+.BR \-\-dylib-id .
 .TP
 .BI \-s " segname sectname"
 The
@@ -173,7 +173,7 @@ The
 option to display the contents of the section
 .RI ( segname,sectname )
 is
-.BI \-section " [segname,]sectname"
+.BI \-\-section " [segname,]sectname"
 where the
 .I segname
 is optional and all segments will be searched for a matching
@@ -184,13 +184,13 @@ The
 .IR objdump (1)
 option to display the contents of the (\_\^\_TEXT,\_\^\_text) section is to use
 the
-.BI \-section " \_\^\_TEXT,\_\^\_text"
+.BI \-\-section " \_\^\_TEXT,\_\^\_text"
 option.  If disassembly is wanted as with the
 .B \-tv
 .IR otool (1)
 options then the
 .IR objdump (1)
-.B \-disassemble
+.B \-\-disassemble
 is used.  By default
 .IR objdump (1)
 also symbolically disassembles the operands as the
@@ -198,7 +198,7 @@ also symbolically disassembles the operands as the
 .B \-tV
 options will do.  If this is not wanted then the
 .IR objdump (1)
-.B \-no-symbolic-operands
+.B \-\-no-symbolic-operands
 option can be used.
 .TP
 .B \-x
@@ -206,14 +206,14 @@ The
 .IR objdump (1)
 option to display the contents of every \_\^\_text section defined in a Mach-O
 file is
-.BI \-section " ,\_\^\_text" .
+.BI \-\-section " ,\_\^\_text" .
 Note that an empty segment name means all segments. If disassembly is wanted as
 with the
 .B \-xv
 .IR otool (1)
 options then
 .IR objdump (1)
-.B \-disassemble-all
+.B \-\-disassemble-all
 is used.  By default
 .IR objdump (1)
 also symbolically disassembles the operands as the
@@ -221,7 +221,7 @@ also symbolically disassembles the operands as the
 .B \-xV
 options will do.  If this is not wanted then the
 .IR objdump (1)
-.B \-no-symbolic-operands
+.B \-\-no-symbolic-operands
 option can be used.
 .TP
 .B \-d
@@ -229,7 +229,7 @@ The
 .IR objdump (1)
 option to display the contents of the (\_\^\_DATA,\_\^\_data) section is to use
 the
-.BI \-section " \_\^\_DATA,\_\^\_data"
+.BI \-\-section " \_\^\_DATA,\_\^\_data"
 option.
 .TP
 .B \-o
@@ -237,7 +237,7 @@ The
 .IR objdump (1)
 option to display the contents of the \_\^\_OBJC segment used by the
 Objective-C run-time system is
-.BR \-objc-meta-data .
+.BR \-\-objc-meta-data .
 .TP
 .B \-r
 The
@@ -251,7 +251,7 @@ and
 When
 .BR \-r
 is combined with the
-.BR \-macho
+.BR \-\-macho
 option,
 .IR objdump (1)
 will display the relocation entries in the classic
@@ -262,26 +262,26 @@ format.
 The
 .IR objdump (1)
 option to display the indirect symbol table is
-.BR \-indirect-symbols .
+.BR \-\-indirect-symbols .
 .TP
 .B \-G
 The
 .IR objdump (1)
 option to display the data in code table is
-.BR \-data-in-code .
+.BR \-\-data-in-code .
 .TP
 .B \-C
 The
 .IR objdump (1)
 option to display the linker optimization hints is
-.BR \-link-opt-hints .
+.BR \-\-link-opt-hints .
 .TP
 .B \-P
 The
 .IR objdump (1)
 option to print the info plist section, (\_\^\_TEXT,\_\^\_info\_plist), as
 strings is
-.BR \-info-plist .
+.BR \-\-info-plist .
 .TP
 .BI "\-p " name
 The
@@ -289,13 +289,13 @@ The
 option to start the disassembly from symbol
 .I name
 is
-.BI \-dis-symname " name".
+.BI \-\-dis-symname " name".
 .TP
 .B \-v
 For
 .IR objdump (1)
 verbose output is the default and to get the non-verbose the option
-.BR \-non-verbose
+.BR \-\-non-verbose
 is used.
 .TP
 .B \-V
@@ -303,29 +303,29 @@ For
 .IR objdump (1)
 displaying the disassembled operands symbolically is the default and to get
 non-symbolic operands with disassembly the option
-.BR \-no-symbolic-operands
+.BR \-\-no-symbolic-operands
 is used.
 .TP
 .B \-X
 The
 .IR objdump (1)
 option to not print leading addresses or headers with disassembly of sections is
-.BR \-no-leading-addr .
+.BR \-\-no-leading-addr .
 .TP
-.BI \-mcpu= arg
+.BI \-\-mcpu= arg
 The
 .IR objdump (1)
 option when doing disassembly using the llvm disassembler to use the cpu
 .I arg.
 is the same
-.BI \-mcpu= arg
+.BI \-\-mcpu= arg
 option.
 .TP
-.BI \-arch " arch_type"
+.BI \-\-arch " arch_type"
 The
 .IR objdump (1)
 options to specifies the architectures to operate on are the same
-.BI \-arch " arch_type"
+.BI \-\-arch " arch_type"
 options.
 .TP
 .B \-j
@@ -333,7 +333,7 @@ The
 .IR objdump (1)
 prints the opcode bytes of the instructions when doing disassembly by
 default and is turned off with the option
-.BR \-no-show-raw-insn .
+.BR \-\-no-show-raw-insn .
 .TP
 .B \-\-version
 The
@@ -343,7 +343,7 @@ option to display the
 version information and the
 .IR objdump (1)
 version information is
-.BR \-version
+.BR \-\-version
 (with one leading dash).
 .SH "OBSOLETE OPTIONS"
 .TP
@@ -395,7 +395,7 @@ option to use the
 .IR otool (1)
 disassembler when doing disassembly as only the llvm disassembler is used.
 .TP
-.B \-function_offsets
+.B \-\-function_offsets
 There is no
 .IR objdump (1)
 option when doing disassembly to print the decimal offset from the last label
@@ -412,4 +412,4 @@ syntax is not supported as file arguments with
 .IR objdump (1).
 .SH "SEE ALSO"
 .IR otool-classic (1),
-.IR llvm-objdump (1)
\ No newline at end of file
+.IR llvm-objdump (1)
