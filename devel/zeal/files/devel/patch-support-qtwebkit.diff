diff --git a/src/libs/browser/CMakeLists.txt b/src/libs/browser/CMakeLists.txt
index 5f242bc..4ec66fe 100644
--- a/src/libs/browser/CMakeLists.txt
+++ b/src/libs/browser/CMakeLists.txt
@@ -1,12 +1,26 @@
-add_library(Browser STATIC
-    searchtoolbar.cpp
-    settings.cpp
-    urlrequestinterceptor.cpp
-    webbridge.cpp
-    webcontrol.cpp
-    webpage.cpp
-    webview.cpp
-)
+if(USE_QTWEBKIT)
+    add_library(Browser STATIC
+        webcontrol.cpp
+        searchtoolbar.cpp
+        webbridge.cpp
+        webview.cpp
+    )
 
-find_package(Qt${QT_VERSION_MAJOR} COMPONENTS WebChannel WebEngineWidgets REQUIRED)
-target_link_libraries(Browser PRIVATE Qt${QT_VERSION_MAJOR}::WebChannel Qt${QT_VERSION_MAJOR}::WebEngineWidgets)
+    target_link_libraries(Browser)
+
+    find_package(Qt5 COMPONENTS WebKitWidgets REQUIRED)
+    target_link_libraries(Browser Qt5::WebKitWidgets)
+else()
+    add_library(Browser STATIC
+        searchtoolbar.cpp
+        settings.cpp
+        urlrequestinterceptor.cpp
+        webbridge.cpp
+        webcontrol.cpp
+        webpage.cpp
+        webview.cpp
+    )
+
+    find_package(Qt${QT_VERSION_MAJOR} COMPONENTS WebChannel WebEngineWidgets REQUIRED)
+    target_link_libraries(Browser PRIVATE Qt${QT_VERSION_MAJOR}::WebChannel Qt${QT_VERSION_MAJOR}::WebEngineWidgets)
+endif()
diff --git a/src/libs/browser/searchtoolbar.cpp b/src/libs/browser/searchtoolbar.cpp
index 5581857..01ca63c 100644
--- a/src/libs/browser/searchtoolbar.cpp
+++ b/src/libs/browser/searchtoolbar.cpp
@@ -10,12 +10,21 @@
 #include <QLineEdit>
 #include <QStyle>
 #include <QToolButton>
+#ifdef USE_WEBKIT
+#include <QWebPage>
+#include <QWebView>
+using QWEBPAGE = QWebPage;
+using QWEBVIEW = QWebView;
+#else
 #include <QWebEnginePage>
 #include <QWebEngineView>
+using QWEBPAGE = QWebEnginePage;
+using QWEBVIEW = QWebEngineView;
+#endif
 
 using namespace Zeal::Browser;
 
-SearchToolBar::SearchToolBar(QWebEngineView *webView, QWidget *parent)
+SearchToolBar::SearchToolBar(QWEBVIEW *webView, QWidget *parent)
     : QWidget(parent)
     , m_webView(webView)
 {
@@ -118,6 +127,7 @@ bool SearchToolBar::eventFilter(QObject *object, QEvent *event)
         case Qt::Key_Up:
         case Qt::Key_PageDown:
         case Qt::Key_PageUp:
+            QCoreApplication::sendEvent(m_webView, event);
             QCoreApplication::sendEvent(m_webView->focusProxy(), event);
             return true;
         default:
@@ -125,6 +135,7 @@ bool SearchToolBar::eventFilter(QObject *object, QEvent *event)
         }
     }
 
+
     return QWidget::eventFilter(object, event);
 }
 
@@ -155,8 +166,12 @@ void SearchToolBar::findNext()
         return;
     }
 
+#ifdef USE_WEBKIT
+    QWebPage::FindFlags ff = QWebPage::FindWrapsAroundDocument;
+#else
     QWebEnginePage::FindFlags ff;
-    ff.setFlag(QWebEnginePage::FindCaseSensitively, m_matchCaseButton->isChecked());
+#endif
+    ff.setFlag(QWEBPAGE::FindCaseSensitively, m_matchCaseButton->isChecked());
     m_webView->findText(m_lineEdit->text(), ff);
 }
 
@@ -166,15 +181,23 @@ void SearchToolBar::findPrevious()
         return;
     }
 
+#ifdef USE_WEBKIT
+    QWebPage::FindFlags ff = QWebPage::FindWrapsAroundDocument;
+#else
     QWebEnginePage::FindFlags ff;
-    ff.setFlag(QWebEnginePage::FindCaseSensitively, m_matchCaseButton->isChecked());
-    ff.setFlag(QWebEnginePage::FindBackward);
+#endif
+    ff.setFlag(QWEBPAGE::FindCaseSensitively, m_matchCaseButton->isChecked());
+    ff.setFlag(QWEBPAGE::FindBackward);
     m_webView->findText(m_lineEdit->text(), ff);
 }
 
 void SearchToolBar::hideHighlight()
 {
+#ifdef USE_WEBKIT
+    m_webView->findText(QString(), QWebPage::HighlightAllOccurrences);
+#else
     m_webView->findText(QString());
+#endif
 }
 
 void SearchToolBar::updateHighlight()
@@ -182,8 +205,12 @@ void SearchToolBar::updateHighlight()
     hideHighlight();
 
     if (m_highlightAllButton->isChecked()) {
+#ifdef USE_WEBKIT
+        QWebPage::FindFlags ff = QWebPage::HighlightAllOccurrences;
+#else
         QWebEnginePage::FindFlags ff;
-        ff.setFlag(QWebEnginePage::FindCaseSensitively, m_matchCaseButton->isChecked());
+#endif
+        ff.setFlag(QWEBPAGE::FindCaseSensitively, m_matchCaseButton->isChecked());
         m_webView->findText(m_lineEdit->text(), ff);
     }
 }
diff --git a/src/libs/browser/searchtoolbar.h b/src/libs/browser/searchtoolbar.h
index 8562f8d..9b7fee5 100644
--- a/src/libs/browser/searchtoolbar.h
+++ b/src/libs/browser/searchtoolbar.h
@@ -8,7 +8,12 @@
 
 class QLineEdit;
 class QToolButton;
+#ifdef USE_WEBKIT
+class QWebView;
+#else
 class QWebEngineView;
+using QWEBVIEW = QWebEngineView;
+#endif
 
 namespace Zeal {
 namespace Browser {
@@ -18,7 +23,11 @@ class SearchToolBar final : public QWidget
     Q_OBJECT
     Q_DISABLE_COPY_MOVE(SearchToolBar)
 public:
+#ifdef USE_WEBKIT
+    explicit SearchToolBar(QWebView *webView, QWidget *parent = nullptr);
+#else
     explicit SearchToolBar(QWebEngineView *webView, QWidget *parent = nullptr);
+#endif
 
     void setText(const QString &text);
     void activate();
@@ -44,7 +53,11 @@ private:
     QToolButton *m_highlightAllButton = nullptr;
     QToolButton *m_matchCaseButton = nullptr;
 
+#ifdef USE_WEBKIT
+    QWebView *m_webView = nullptr;
+#else
     QWebEngineView *m_webView = nullptr;
+#endif
 };
 
 } // namespace Browser
diff --git a/src/libs/browser/webcontrol.cpp b/src/libs/browser/webcontrol.cpp
index acf00b6..f02c148 100644
--- a/src/libs/browser/webcontrol.cpp
+++ b/src/libs/browser/webcontrol.cpp
@@ -12,10 +12,17 @@
 #include <QDataStream>
 #include <QKeyEvent>
 #include <QVBoxLayout>
+#ifdef USE_WEBKIT
+#include <QStyle>
+#include <QWebFrame>
+#include <QWebHistory>
+#include <QWebPage>
+#else
 #include <QWebChannel>
 #include <QWebEngineHistory>
 #include <QWebEnginePage>
 #include <QWebEngineSettings>
+#endif
 
 using namespace Zeal::Browser;
 
@@ -29,15 +36,28 @@ WebControl::WebControl(QWidget *parent)
     m_webView = new WebView();
     setFocusProxy(m_webView);
 
-    connect(m_webView->page(), &QWebEnginePage::linkHovered, this, [this](const QString &link) {
+#ifdef USE_WEBKIT
+    connect(m_webView->page(), &QWebPage::linkHovered, this, [this](const QString &link)
+#else
+    connect(m_webView->page(), &QWebEnginePage::linkHovered, this, [this](const QString &link)
+#endif
+    {
         if (Core::NetworkAccessManager::isLocalUrl(QUrl(link))) {
             return;
         }
 
+        setToolTip(link);
+#ifdef USE_WEBKIT
         m_webView->setToolTip(link);
+#endif
     });
+#ifdef USE_WEBKIT
+    connect(m_webView, &QWebView::titleChanged, this, &WebControl::titleChanged);
+    connect(m_webView, &QWebView::urlChanged, this, &WebControl::urlChanged);
+#else
     connect(m_webView, &QWebEngineView::titleChanged, this, &WebControl::titleChanged);
     connect(m_webView, &QWebEngineView::urlChanged, this, &WebControl::urlChanged);
+#endif
 
     layout->addWidget(m_webView);
 
@@ -76,22 +96,34 @@ void WebControl::resetZoom()
 
 void WebControl::setJavaScriptEnabled(bool enabled)
 {
+#ifdef USE_WEBKIT
+    m_webView->page()->settings()->setAttribute(QWebSettings::JavascriptEnabled, enabled);
+#else
     m_webView->page()->settings()->setAttribute(QWebEngineSettings::JavascriptEnabled, enabled);
+#endif
 }
 
 void WebControl::setWebBridgeObject(const QString &name, QObject *object)
 {
+#ifdef USE_WEBKIT
+    connect(m_webView->page()->mainFrame(), &QWebFrame::javaScriptWindowObjectCleared,
+            this, [=]() {
+        m_webView->page()->mainFrame()->addToJavaScriptWindowObject(name, object);
+    });
+#else
     QWebEnginePage *page = m_webView->page();
 
     QWebChannel *channel = new QWebChannel(page);
     channel->registerObject(name, object);
 
     page->setWebChannel(channel);
+#endif
 }
 
 void WebControl::load(const QUrl &url)
 {
     m_webView->load(url);
+    m_webView->setFocus();
 }
 
 void WebControl::activateSearchBar()
@@ -141,7 +173,11 @@ QUrl WebControl::url() const
     return m_webView->url();
 }
 
+#ifdef USE_WEBKIT
+QWebHistory *WebControl::history() const
+#else
 QWebEngineHistory *WebControl::history() const
+#endif
 {
     return m_webView->history();
 }
diff --git a/src/libs/browser/webcontrol.h b/src/libs/browser/webcontrol.h
index 877c0a1..10948c6 100644
--- a/src/libs/browser/webcontrol.h
+++ b/src/libs/browser/webcontrol.h
@@ -7,7 +7,11 @@
 
 #include <QWidget>
 
+#ifdef USE_WEBKIT
+class QWebHistory;
+#else
 class QWebEngineHistory;
+#endif
 
 namespace Zeal {
 namespace Browser {
@@ -30,7 +34,11 @@ public:
     QString title() const;
     QUrl url() const;
 
+#ifdef USE_WEBKIT
+    QWebHistory *history() const;
+#else
     QWebEngineHistory *history() const;
+#endif
     void restoreHistory(const QByteArray &array);
     QByteArray saveHistory() const;
 
diff --git a/src/libs/browser/webview.cpp b/src/libs/browser/webview.cpp
index f13fc73..e1b64a2 100644
--- a/src/libs/browser/webview.cpp
+++ b/src/libs/browser/webview.cpp
@@ -5,7 +5,9 @@
 #include "webview.h"
 
 #include "webcontrol.h"
+#ifndef USE_WEBKIT
 #include "webpage.h"
+#endif
 
 #include <core/application.h>
 #include <core/settings.h>
@@ -18,29 +20,39 @@
 #include <QMenu>
 #include <QMessageBox>
 #include <QPushButton>
+#ifndef USE_WEBKIT
 #include <QVector>
 #include <QWebEngineProfile>
 #include <QWebEngineSettings>
+#endif
 #include <QWheelEvent>
 
+#ifndef USE_WEBKIT
 #if QT_VERSION < QT_VERSION_CHECK(6, 0, 0)
 #include <QWebEngineContextMenuData>
 #else
 #include <QWebEngineContextMenuRequest>
 #endif
+#endif //USE_WEBKIT
 
 using namespace Zeal::Browser;
 
 WebView::WebView(QWidget *parent)
-    : QWebEngineView(parent)
+    : QWEBVIEW(parent)
 {
+#ifdef USE_WEBKIT
+    page()->setNetworkAccessManager(Core::Application::instance()->networkManager());
+#else
     setPage(new WebPage(this));
+#endif
     setZoomLevel(defaultZoomLevel());
 
+#ifdef USE_WEBKIT
     // Enable plugins for PDF support.
     settings()->setAttribute(QWebEngineSettings::PluginsEnabled, true);
 
     settings()->setAttribute(QWebEngineSettings::FocusOnNavigationEnabled, false);
+#endif
 
     QApplication::instance()->installEventFilter(this);
 }
@@ -97,7 +109,7 @@ void WebView::resetZoom()
     setZoomLevel(defaultZoomLevel());
 }
 
-QWebEngineView *WebView::createWindow(QWebEnginePage::WebWindowType type)
+QWEBVIEW *WebView::createWindow(QWEBPAGE::WebWindowType type)
 {
     Q_UNUSED(type)
     return Core::Application::instance()->mainWindow()->createTab()->webControl()->m_webView;
@@ -109,15 +121,24 @@ void WebView::contextMenuEvent(QContextMenuEvent *event)
     const QWebEngineContextMenuData& contextData = page()->contextMenuData();
 
     if (!contextData.isValid()) {
-        QWebEngineView::contextMenuEvent(event);
+        QWEBVIEW::contextMenuEvent(event);
+        return;
+    }
+#else
+#ifdef USE_WEBKIT
+    const QWebHitTestResult hitTestResult = hitTestContent(event->pos());
+    // Return standard menu for input fields.
+    if (hitTestResult.isContentEditable()) {
+        QWebView::contextMenuEvent(event);
         return;
     }
 #else
     QWebEngineContextMenuRequest *contextMenuRequest = lastContextMenuRequest();
     if (contextMenuRequest == nullptr) {
-        QWebEngineView::contextMenuEvent(event);
+        QWEBVIEW::contextMenuEvent(event);
         return;
     }
+#endif //USE_WEBKIT
 #endif
 
     event->accept();
@@ -130,6 +151,8 @@ void WebView::contextMenuEvent(QContextMenuEvent *event)
 
 #if QT_VERSION < QT_VERSION_CHECK(6, 0, 0)
     QUrl linkUrl = contextData.linkUrl();
+#elifdef USE_WEBKIT
+    const QUrl linkUrl = hitTestResult.linkUrl();
 #else
     QUrl linkUrl = contextMenuRequest->linkUrl();
 #endif
@@ -139,7 +162,7 @@ void WebView::contextMenuEvent(QContextMenuEvent *event)
 
         if (scheme != QLatin1String("javascript")) {
             m_contextMenu->addAction(tr("Open Link in New Tab"), this, [this]() {
-                triggerPageAction(QWebEnginePage::WebAction::OpenLinkInNewWindow);
+                triggerPageAction(QWEBPAGE::WebAction::OpenLinkInNewWindow);
             });
         }
 
@@ -150,23 +173,26 @@ void WebView::contextMenuEvent(QContextMenuEvent *event)
                 });
             }
 
-            m_contextMenu->addAction(pageAction(QWebEnginePage::CopyLinkToClipboard));
+            m_contextMenu->addAction(pageAction(QWEBPAGE::CopyLinkToClipboard));
         }
     }
 
-
+#ifdef USE_WEBKIT
+    if (hitTestResult.isContentSelected())
+#else // USE_WEBKIT
 #if QT_VERSION < QT_VERSION_CHECK(6, 0, 0)
     const QString selectedText = contextData.selectedText();
 #else
     const QString selectedText = contextMenuRequest->selectedText();
 #endif
-
-    if (!selectedText.isEmpty()) {
+    if (!selectedText.isEmpty())
+#endif // USE_WEBKIT
+    {
         if (!m_contextMenu->isEmpty()) {
             m_contextMenu->addSeparator();
         }
 
-        m_contextMenu->addAction(pageAction(QWebEnginePage::Copy));
+        m_contextMenu->addAction(pageAction(QWEBPAGE::Copy));
     }
 
     if (!linkUrl.isValid() && url().scheme() != QLatin1String("qrc")) {
@@ -174,8 +200,8 @@ void WebView::contextMenuEvent(QContextMenuEvent *event)
             m_contextMenu->addSeparator();
         }
 
-        m_contextMenu->addAction(pageAction(QWebEnginePage::Back));
-        m_contextMenu->addAction(pageAction(QWebEnginePage::Forward));
+        m_contextMenu->addAction(pageAction(QWEBPAGE::Back));
+        m_contextMenu->addAction(pageAction(QWEBPAGE::Forward));
         m_contextMenu->addSeparator();
 
         m_contextMenu->addAction(tr("Open Page in Desktop Browser"), this, [this]() {
@@ -261,5 +287,23 @@ bool WebView::eventFilter(QObject *watched, QEvent *event)
         }
     }
 
-    return QWebEngineView::eventFilter(watched, event);
+    return QWEBVIEW::eventFilter(watched, event);
 }
+
+#ifdef USE_WEBKIT
+QWebHitTestResult WebView::hitTestContent(const QPoint &pos) const
+{
+    return page()->mainFrame()->hitTestContent(pos);
+}
+
+bool WebView::isExternalUrl(const QUrl &url)
+{
+    static const QStringList localSchemes = {
+        QStringLiteral("file"),
+        QStringLiteral("qrc"),
+    };
+
+    const QString scheme = url.scheme();
+    return !scheme.isEmpty() && !localSchemes.contains(scheme);
+}
+#endif
diff --git a/src/libs/browser/webview.h b/src/libs/browser/webview.h
index eb6137d..7d3e6a6 100644
--- a/src/libs/browser/webview.h
+++ b/src/libs/browser/webview.h
@@ -5,12 +5,21 @@
 #ifndef ZEAL_BROWSER_WEBVIEW_H
 #define ZEAL_BROWSER_WEBVIEW_H
 
+#ifdef USE_WEBKIT
+#include <QWebFrame>
+#include <QWebView>
+using QWEBVIEW = QWebView;
+using QWEBPAGE = QWebPage;
+#else
 #include <QWebEngineView>
+using QWEBVIEW = QWebEngineView;
+using QWEBPAGE = QWebEnginePage;
+#endif
 
 namespace Zeal {
 namespace Browser {
 
-class WebView final : public QWebEngineView
+class WebView final : public QWEBVIEW
 {
     Q_OBJECT
     Q_DISABLE_COPY_MOVE(WebView)
@@ -34,10 +43,14 @@ signals:
     void zoomLevelChanged();
 
 protected:
-    QWebEngineView *createWindow(QWebEnginePage::WebWindowType type) override;
+    QWEBVIEW *createWindow(QWEBPAGE::WebWindowType type) override;
     void contextMenuEvent(QContextMenuEvent *event) override;
 
 private:
+#ifdef USE_WEBKIT
+    QWebHitTestResult hitTestContent(const QPoint &pos) const;
+    static bool isExternalUrl(const QUrl &url);
+#endif
     bool handleMouseReleaseEvent(QMouseEvent *event);
     bool handleWheelEvent(QWheelEvent *event);
 
