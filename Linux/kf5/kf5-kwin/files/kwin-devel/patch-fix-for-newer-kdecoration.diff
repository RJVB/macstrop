diff --git a/decorations/decoratedclient.cpp b/decorations/decoratedclient.cpp
index 09892cfa7b..ae99059c06 100644
--- a/decorations/decoratedclient.cpp
+++ b/decorations/decoratedclient.cpp
@@ -67,8 +67,7 @@ DecoratedClientImpl::DecoratedClientImpl(AbstractClient *client, KDecoration2::D
             if (oldSize.height() != m_clientSize.height()) {
                 emit decoratedClient->heightChanged(m_clientSize.height());
             }
-#if KDECORATION2_VERSION <= QT_VERSION_CHECK(5, 15, 5)
-// this whole KCM implementation comes from KWin 5.15.5
+#if KDECORATION2_VERSION >= QT_VERSION_CHECK(5, 18, 0)
             emit decoratedClient->sizeChanged(m_clientSize);
 #else
             emit sizeChanged(m_clientSize);
diff --git a/decorations/decoratedclient.h b/decorations/decoratedclient.h
index 5ebb9337d4..f35fc8c949 100644
--- a/decorations/decoratedclient.h
+++ b/decorations/decoratedclient.h
@@ -68,8 +68,7 @@ public:
     QPalette palette() const override;
     QColor color(KDecoration2::ColorGroup group, KDecoration2::ColorRole role) const override;
     bool providesContextHelp() const override;
-#if KDECORATION2_VERSION <= QT_VERSION_CHECK(5, 15, 5)
-    // this whole KCM implementation comes from KWin 5.15.5 !
+#if KDECORATION2_VERSION >= QT_VERSION_CHECK(5, 18, 0)
     QSize size() const override;
 #else
     QSize size() const;
@@ -109,7 +108,7 @@ public:
 
     void signalShadeChange();
 
-#if KDECORATION2_VERSION > QT_VERSION_CHECK(5, 15, 5)
+#if KDECORATION2_VERSION < QT_VERSION_CHECK(5, 18, 0)
 Q_SIGNALS:
     void sizeChanged(QSize&);
 #endif
diff --git a/decorations/decorationrenderer.cpp b/decorations/decorationrenderer.cpp
index 713400ad67..6ea0df9549 100644
--- a/decorations/decorationrenderer.cpp
+++ b/decorations/decorationrenderer.cpp
@@ -45,8 +45,7 @@ Renderer::Renderer(DecoratedClientImpl *client)
     };
     connect(client->client(), &AbstractClient::screenScaleChanged, this, markImageSizesDirty);
     connect(client->decoration(), &KDecoration2::Decoration::bordersChanged, this, markImageSizesDirty);
-#if KDECORATION2_VERSION <= QT_VERSION_CHECK(5, 15, 5)
-// this whole KCM implementation comes from KWin 5.15.5
+#if KDECORATION2_VERSION >= QT_VERSION_CHECK(5, 18, 0)
     connect(client->decoratedClient(), &KDecoration2::DecoratedClient::sizeChanged, this, markImageSizesDirty);
 #endif
 }
diff --git a/kcmkwin/kwindecoration/declarative-plugin/previewclient.cpp b/kcmkwin/kwindecoration/declarative-plugin/previewclient.cpp
index c5856ebd5f..fadcb07c53 100644
--- a/kcmkwin/kwindecoration/declarative-plugin/previewclient.cpp
+++ b/kcmkwin/kwindecoration/declarative-plugin/previewclient.cpp
@@ -79,6 +79,9 @@ PreviewClient::PreviewClient(DecoratedClient *c, Decoration *decoration)
     connect(this, &PreviewClient::onAllDesktopsChanged,         c, &DecoratedClient::onAllDesktopsChanged);
     connect(this, &PreviewClient::widthChanged,                 c, &DecoratedClient::widthChanged);
     connect(this, &PreviewClient::heightChanged,                c, &DecoratedClient::heightChanged);
+#if KDECORATION2_VERSION >= QT_VERSION_CHECK(5, 18, 0)
+    connect(this, &PreviewClient::sizeChanged,                  c, &DecoratedClient::sizeChanged);
+#endif
     connect(this, &PreviewClient::iconChanged,                  c, &DecoratedClient::iconChanged);
     connect(this, &PreviewClient::paletteChanged,               c, &DecoratedClient::paletteChanged);
 //         connect(this, &PreviewClient::, c, &DecoratedClient::);
@@ -135,6 +138,12 @@ int PreviewClient::height() const
     return m_height;
 }
 
+QSize PreviewClient::size() const
+{
+    QSize size(m_width, m_height);
+    return size;
+}
+
 QString PreviewClient::caption() const
 {
     return m_caption;
diff --git a/kcmkwin/kwindecoration/declarative-plugin/previewclient.h b/kcmkwin/kwindecoration/declarative-plugin/previewclient.h
index df3608393e..39834d2ba8 100644
--- a/kcmkwin/kwindecoration/declarative-plugin/previewclient.h
+++ b/kcmkwin/kwindecoration/declarative-plugin/previewclient.h
@@ -26,6 +26,8 @@
 #include <QObject>
 #include <QPalette>
 
+#include <kdecoration2_version.h>
+
 class QAbstractItemModel;
 
 namespace KDecoration2
@@ -90,6 +92,11 @@ public:
 
     int width() const override;
     int height() const override;
+#if KDECORATION2_VERSION >= QT_VERSION_CHECK(5, 18, 0)
+    QSize size() const override;
+#else
+    QSize size() const;
+#endif
     QPalette palette() const override;
     QColor color(ColorGroup group, ColorRole role) const override;
     Qt::Edges adjacentScreenEdges() const override;
@@ -110,7 +117,11 @@ public:
     void requestShowApplicationMenu(const QRect &rect, int actionId) override;
     void requestToggleOnAllDesktops() override;
 
+#if KDECORATION2_VERSION >= QT_VERSION_CHECK(5, 18, 0)
+    void showApplicationMenu(int actionId) override;
+#else
     void showApplicationMenu(int actionId);
+#endif
 
     void setCaption(const QString &caption);
     void setActive(bool active);
@@ -169,6 +180,7 @@ Q_SIGNALS:
     void desktopChanged(int);
     void widthChanged(int);
     void heightChanged(int);
+    void sizeChanged(QSize&);
     void paletteChanged(const QPalette&);
     void bordersTopEdgeChanged(bool);
     void bordersLeftEdgeChanged(bool);
