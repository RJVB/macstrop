# Tcl package index file, version 1.1
# This file is installed by port:MacPorts-pextlib and
# intended used to replace the original pkgIndex.tcl
# file left by the MacPorts installer.

set pextlib [file join $dir Pextlib-mp.dylib]
if {![file exists ${pextlib}]} {
	set pextlib [file join $dir Pextlib.dylib]
}
package ifneeded Pextlib 1.0 [list load [file join $dir ${pextlib}]]
