--- orig.configure	2016-01-12 19:21:24.000000000 +0100
+++ configure	2016-12-25 21:13:34.000000000 +0100
@@ -1500,7 +1500,7 @@
 Optional Packages:
   --with-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
   --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
-  --with-mdds=1.0|0.x     Specify which version of mdds to use (1.0 is the
+  --with-mdds=1.2|0.x     Specify which version of mdds to use (1.2 is the
                           default)
   --with-pic[=PKGS]       try to use only PIC/non-PIC objects [default=use
                           both]
@@ -3155,7 +3155,7 @@
 if test "${with_mdds+set}" = set; then :
   withval=$with_mdds;
 else
-  with_mdds="1.0"
+  with_mdds="1.2"
 fi
 
 
@@ -17459,7 +17459,7 @@
 GPERF=${GPERF-"${am_missing_run}gperf"}
 
 
-if test "$with_mdds" = "1.0"; then :
+if test "$with_mdds" = "1.2"; then :
       ax_cxx_compile_cxx11_required=true
   ac_ext=cpp
 ac_cpp='$CXXCPP $CPPFLAGS'
@@ -18242,7 +18242,7 @@
 # =========
 # Find mdds
 # =========
-if test "$with_mdds" = "1.0"; then :
+if test "$with_mdds" = "1.2"; then :
 
 
 pkg_failed=no
@@ -18253,12 +18253,12 @@
     pkg_cv_MDDS_CFLAGS="$MDDS_CFLAGS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"mdds-1.0\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "mdds-1.0") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"mdds-1.2\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "mdds-1.2") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_MDDS_CFLAGS=`$PKG_CONFIG --cflags "mdds-1.0" 2>/dev/null`
+  pkg_cv_MDDS_CFLAGS=`$PKG_CONFIG --cflags "mdds-1.2" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -18270,12 +18270,12 @@
     pkg_cv_MDDS_LIBS="$MDDS_LIBS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"mdds-1.0\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "mdds-1.0") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"mdds-1.2\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "mdds-1.2") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_MDDS_LIBS=`$PKG_CONFIG --libs "mdds-1.0" 2>/dev/null`
+  pkg_cv_MDDS_LIBS=`$PKG_CONFIG --libs "mdds-1.2" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -18296,14 +18296,14 @@
         _pkg_short_errors_supported=no
 fi
         if test $_pkg_short_errors_supported = yes; then
-	        MDDS_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "mdds-1.0" 2>&1`
+	        MDDS_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "mdds-1.2" 2>&1`
         else
-	        MDDS_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "mdds-1.0" 2>&1`
+	        MDDS_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "mdds-1.2" 2>&1`
         fi
 	# Put the nasty error message in config.log where it belongs
 	echo "$MDDS_PKG_ERRORS" >&5
 
-	as_fn_error $? "Package requirements (mdds-1.0) were not met:
+	as_fn_error $? "Package requirements (mdds-1.2) were not met:
 
 $MDDS_PKG_ERRORS
 
